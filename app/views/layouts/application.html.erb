<!DOCTYPE html>
<html>
  <head>
    <title>Devise use</title>
  
  <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">

  </head>

  <body style="background-color:powderblue;">
    
  <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  <a class="navbar-brand" href="#">Gaurav's Website</a>
  <div>
    <% if user_signed_in? %>
      <p class="navbar-text mb-0 mr-3"><strong><%= current_user.email %></strong></p>
      <%= link_to 'Edit Profile', edit_user_registration_path, class: 'btn btn-outline-primary' %>
      <%= link_to 'Log out', destroy_user_session_path,  data: { "turbo-method": :delete }, class: 'btn btn-outline-danger ml-2' %>
    <% else %>
      <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-outline-success' %>
      <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-primary ml-2' %>
    <% end %>
  </div>
</nav>
    <!--<p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>-->
    <%= yield %>
  </body>
</html>